# Omgevingsvariabelen — vul lokaal in via .env (nooit committen).
# Neon Postgres connection strings (pooled + unpooled) en NextAuth secret.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
AUTH_SECRET=

# Alleen gebruikt door `npm run seed:admin` (src/scripts/seed-admin.ts).
ADMIN_EMAIL=
ADMIN_PASSWORD=
